import numpy as np
from numpy import pi

x = np.sinh(pi/2)
print(x)

arr = np.array([pi/2, pi/3, pi/4, pi/5])
print(np.cosh(arr))

# finding angles
x = np.arcsinh(1.0)
print(x)

arr = np.array([0.1, 0.2, 0.3])
print(np.arctanh(arr))